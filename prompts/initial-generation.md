Create the first complete website for the supplied target path. The user description is optional. A parent snapshot, if present, offers layout, language, navigation, and content context; adapt it to the new page's purpose instead of copying irrelevant facts. Prioritize the path and explicit user description over parent conventions.

Aim for one complete submission when no external assets are needed. Make the result readable on both a narrow phone and a desktop. If the user explicitly wants a deliberate layout experiment, follow that request.
